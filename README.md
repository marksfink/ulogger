# ulogger

Logs messages to a Mac's unified logging system with support for subsystem, category, and type arguments.

I wrote this as a replacement for logger when I discovered that logger's tag argument does nothing on a Mac and filtering/finding logs (that are written with logger) is difficult-to-impossible.
